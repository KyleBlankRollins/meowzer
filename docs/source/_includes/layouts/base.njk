<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }} | Meowzer</title>
    <meta name="description" content="{{ description }}" />
    
    {# Styles - Vite will process these automatically #}
    <link rel="stylesheet" href="{{ '/styles/main.css' | url }}" />
  </head>
  <body>
    {# Navigation component with top-level navigation links #}
    <mb-nav
      data-links='{{ collections.topNav | dump | safe }}'
      data-current-url="{{ page.url }}"
      data-site-title="Meowzer 🐱"
    ></mb-nav>
    
    {# Main content from child layouts #}
    {{ content | safe }}
    
    {# Scripts - Vite will process these automatically #}
    <script type="module" src="{{ '/scripts/main.js' | url }}"></script>
  </body>
</html>
