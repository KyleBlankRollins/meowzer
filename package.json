{
  "name": "meowzer-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "meowzer/sdk",
    "meowzer/ui",
    "docs",
    "demo"
  ],
  "scripts": {
    "dev:docs": "npm run dev --workspace=docs",
    "build:docs": "npm run build --workspace=docs",
    "dev:demo": "npm run dev --workspace=demo",
    "build:demo": "npm run build:sdk && npm run build:ui && npm run build --workspace=demo",
    "build:sdk": "npm run build --workspace=meowzer/sdk",
    "build:ui": "npm run build --workspace=meowzer/ui",
    "build": "npm run build:sdk && npm run build:ui"
  },
  "devDependencies": {},
  "description": "Monorepo containing Meowzer SDK, UI components, documentation, and demo site"
}
